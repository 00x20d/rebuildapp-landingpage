services:
  rebuild-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rebuild-landing
    restart: unless-stopped
    ports:
      - "127.0.0.1:3003:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      PORT: 3000
